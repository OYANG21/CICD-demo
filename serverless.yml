service: cicd-demo

plugins:
  - serverless-python-requirements

provider:
  name: aws
  runtime: python3.7
  region: us-east-1

  deploymentBucket:
    serverSideEncryption: AES256
    blockPublicAccess: true

  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:Query 
        - dynamodb:GetItem 
        - dynamodb:PutItem     
        - dynamodb:UpdateItem 
        - dynamodb:DeleteItem
        - lambda:*
        - log:*
      Resource:
        - '*'
        #- arn:aws:dynamo:*account:table/name*

custom:
  pythonRequirements:
    dockerizePip: true
    dynamodb:
      stages:
        - dev
      start:
        migrate: true

package:
  individually: false
  exclude:
    - package.json
    - package-log.json
    - node_modules/**

functions:
  add_new:
    handler: handler.lambda_handler
    timeout: 5
    events:
      - http:
          integration: lambda
          path: add_new
          cors: false
          method: post
          request:
            passThrough: NEVER

resources:
  Resources:
    ChallengeDB:
      Type: 'AWS::DynamoDB::Table'
      Properties:
        AttributeDefinitions:
          - AttributeName: team_country
            AttributeType: S
          - AttributeName: team_name
            AttributeType: S
          - AttributeName: team_desc
            AttributeType: S
          - AttributeName: team_rating
            AttributeType: S
        KeySchema:
          - 
            AttributeName: team_country
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST
        TableName: Challenge_DB

